import 'package:flutter/material.dart';

class CarIcon extends StatelessWidget {
  const CarIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_car, color: Colors.indigo, size: 32);
  }
}

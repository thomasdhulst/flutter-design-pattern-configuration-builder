import 'package:flutter/material.dart';

class BikeIcon extends StatelessWidget {
  const BikeIcon({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_bike, color: Colors.green, size: 32);
  }
}
